import React from 'react';
import css from './Settings.module.css';

const Settings = () => {
	return (
		<div className={`${css.App__settings}`}>
			Настройки
		</div>
	)
}

export default Settings;
